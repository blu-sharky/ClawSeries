# ClawSeries Backend

Python 后端服务，提供 RESTful API 和 WebSocket 实时通信。

## 运行方式

```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

## API 文档

启动后访问 http://localhost:8000/docs 查看 Swagger 文档。
